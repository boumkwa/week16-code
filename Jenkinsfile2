pipeline{
    agent any
    tools{
        maven 'M2_HOME'
    }
    stages{
        stage('maven command'){
            steps{
            sh 'mnv clean compile install package'
            }
        }
    }
}